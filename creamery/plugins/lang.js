export default function (context, _inject) {
  // TODO Nuxt i18n sets cookie expiration 1d by default
  // We could read the cookie here, and re-set expiration to 365d,
  // same as we do it inside LanguageInput component
}
